# Optional first-run convenience only.
# If set, the key is moved into the macOS Keychain on first launch and this
# value is never persisted by the app. You can also paste the key into the
# in-app banner instead. Never commit a real key — .env is gitignored.
ANTHROPIC_API_KEY=
